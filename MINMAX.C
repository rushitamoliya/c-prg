#include<stdio.h>
#include<conio.h>
void main()
{
int x,y;
clrscr();
	 printf("enter value\n");
	 scanf("%d",&x);
	 printf("eneter value\n");
	 scanf("%d",&y);
	 if(x<y)
	 {
	 printf("enter minimum number is %d",x);
	 }
	 else
	 {
	 printf("eneter minimum  number is %d",y);
	 }
getch();
}